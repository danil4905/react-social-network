import React from 'react';
import preloader from "./freloader.svg";

let Preloader = (props) => {
    return (
        <div class="preloader">
            <img src={preloader} />
        </div>)
}
export default Preloader;